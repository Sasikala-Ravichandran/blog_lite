<div class="main_wrapper">
  <div class="sub_header">
    <%= link_to "Home", current_user, :class => "right buttons" %><br>
    <%= link_to "New Post", new_user_post_path(current_user), class: "middle buttons" if current_user.admin? %>
  </div>
  <div class="center_it top_margin">
    <h2 class="title">New Category</h2>
    <%= render 'form'%>
  </div>
</div>
      