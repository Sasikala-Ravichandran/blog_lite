<%= form_for(@post.comments.build, html: { align: "center" }, remote: true) do |f| %>
  <%= f.hidden_field :post_id, :value => @post.id %>
  <%= f.text_area :content, placeholder: "Write a comment..." %><br>
  <br>
  <%= f.submit "Post comment", id: "zero_margin" %>
<% end %>